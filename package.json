{
  "name": "meowcal-sub",
  "version": "0.6.8",
  "description": "NPU-accelerated subtitle translation for Copilot+ PCs",
  "private": true,
  "engines": {
    "node": ">=24 <25",
    "npm": ">=11 <12"
  },
  "scripts": {
    "dev:browser": "vite --host 127.0.0.1 --port 3000 --strictPort",
    "dev:tauri-web": "vite --host 127.0.0.1 --port 1420 --strictPort",
    "build:web": "vite build",
    "version:check": "node scripts/check-version-sync.mjs",
    "runners:check": "node scripts/check-workflow-runners.mjs",
    "typecheck": "tsc --noEmit",
    "prepare:validation": "pwsh -NoProfile -File scripts/prepare-validation-resources.ps1",
    "dev:backend": "npm run prepare:validation && cd src-tauri && cargo run --locked -- --http-only",
    "format:check": "prettier --check \"src/**/*.{js,ts,css,html}\" \"frontend-tests/**/*.{js,mjs,ts}\" \"scripts/*.mjs\" \"config/**/*.json\" \"*.{js,json,mjs,ts}\"",
    "lint": "node scripts/run-eslint.mjs",
    "maintainability": "node scripts/check-maintainability.mjs",
    "eval:subtitles": "pwsh -NoProfile -File scripts/run-subtitle-eval.ps1",
    "test:frontend": "vitest run --coverage",
    "test:browser": "playwright test",
    "frontend:check": "npm run version:check && npm run runners:check && npm run format:check && npm run lint && npm run typecheck && npm run build:web && npm run test:frontend && npm run test:browser",
    "tauri": "tauri",
    "tauri:dev": "tauri dev",
    "tauri:build": "tauri build"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "@playwright/test": "1.62.0",
    "@tauri-apps/cli": "2.11.4",
    "@types/node": "^26.1.2",
    "@vitest/coverage-v8": "4.1.10",
    "eslint": "10.8.0",
    "globals": "17.8.0",
    "prettier": "3.9.6",
    "typescript": "^6.0.2",
    "typescript-eslint": "^8.65.0",
    "vite": "^8.2.0",
    "vitest": "4.1.10"
  },
  "keywords": [
    "tauri",
    "subtitle",
    "translation",
    "ocr",
    "copilot-plus"
  ],
  "author": "PeterShanxin",
  "license": "MIT",
  "dependencies": {
    "@phosphor-icons/web": "^2.1.2",
    "lit": "^3.3.3"
  }
}
