# Change Contract

One contract for how a change is named, versioned, and proposed. It covers
commit messages, product version ownership, and pull request metadata.
`CONTRIBUTING.md` and `docs/AGENT_GUIDE.md` link here rather than restating it,
so there is exactly one place to change a rule.

CI enforces the mechanical parts in the **Change Contract** job. Everything that
CI cannot judge - whether the stated non-goals are honest, whether the manual
gate really passed - is a review responsibility and is written as such.

## Commit messages

```text
<type>(<optional scope>)<optional !>: <subject>
```

- **type** is one of `build`, `chore`, `ci`, `docs`, `feat`, `fix`, `perf`,
  `refactor`, `revert`, `style`, `test`.
- **scope** is optional, lowercase, and names the owner the change belongs to -
  a module, a script, an area - using letters, digits, `.`, `_`, `/`, and `-`.
- **`!`** marks a breaking change and requires a `BREAKING CHANGE: <consequence>`
  footer in the body.
- **subject** is a single line, does not end with a period, and the whole header
  stays within 100 characters.
- The body is optional and free-form. Reference the owning issue in the subject
  or the body; `(#34)` in the subject is the established style here.

Valid:

```text
fix: keep the capture region and its scale factor atomic to readers
refactor(overlay): give the timers and appearance state their own owners (#34)
docs: record engine_status as readiness orchestration owner
feat!: remove the developer-mode generic endpoint

BREAKING CHANGE: saved developer-mode endpoints are ignored after upgrade.
```

Invalid:

```text
Update overlay.js                 # no type
fix:keep the overlay visible      # no space after the colon
wip: half a change                # 'wip' is not an allowed type
fix(Overlay): keep it visible     # scope is not lowercase
fix: keep the overlay visible.    # trailing period
feat!: drop the legacy endpoint   # '!' without a BREAKING CHANGE footer
```

### Exemptions

- **Merge commits.** GitHub generates their subject, and this contract does not
  ask anyone to hand-edit it.
- **Reverts.** GitHub's revert button writes `Revert "<original subject>"`,
  which is accepted as-is. A hand-written revert uses `revert:`.
- **Dependency bots.** Commits and pull requests authored by a `[bot]` identity
  are exempt, because the contributor being blocked cannot reword them.

### What is checked, and when

The check reads only the commits a pull request **adds** to its base. No commit
already on `main` is validated, and no published history is ever rewritten to
satisfy this contract.

Locally, before opening a pull request:

```powershell
npm run commits:check                       # against origin/main
node scripts/check-commit-contract.mjs --base origin/main --head HEAD
```

That local run is feedback, not the gate. CI is the gate. A commit-msg hook is
allowed as personal tooling and must never be the only thing enforcing this.

## Product version ownership

`src-tauri/tauri.conf.json` holds the product version. Five files must agree and
`scripts/check-version-sync.mjs` fails the build when they do not:

| File | Role |
|---|---|
| `src-tauri/tauri.conf.json` | the record; the installer and updater read it |
| `package.json` | copy |
| `package-lock.json` (both `version` fields) | copy, regenerated by npm |
| `src-tauri/Cargo.toml` | copy |
| `src-tauri/Cargo.lock` | copy, regenerated by cargo |

Rules:

- change all five in one pull request, with a `chore(release): prepare vX.Y.Z`
  commit that changes nothing else;
- never bump a version as a side effect of a feature or fix;
- `npm run version:check -- --expected 0.6.9` asserts an exact value when a
  release workflow needs to prove which version it is building.

Releasing itself stays manual and deliberate: see the release and update
contract in `docs/AGENT_GUIDE.md`. This repository has no automatic versioning
and none is planned under Epic #36.

## Pull request titles

A pull request title uses the **same grammar as a commit subject**, and CI
checks it. It is not decoration: with the repository's merge settings the title
becomes the body of the merge commit on `main`, so it is the sentence a later
reader sees next to `Merge pull request #N`.

```text
refactor: give the overlay timers and appearance state their own owners (#34)
```

## Pull request descriptions

`.github/pull_request_template.md` is the template. Keep every section, delete
the guidance comments, and keep the answers short.

- **Intent** - the problem, in a sentence or two.
- **Changes** - what this pull request actually touches.
- **Non-goals** - what a reviewer might expect and will not find here.
- **Validation** - the automated evidence. Name the command and its result.
  A performance claim needs comparable before/after numbers measured the same
  way, or it is not made. A release build is not comparable to a dev build.
- **Manual Windows validation** - capture, OCR, overlay, selector, tray, DPI,
  installer, and lifecycle behavior are not proven by automated tests. State
  the commit tested, the architecture and Windows build, the scenario, and the
  observed result - or state plainly that the gate is outstanding. Attach a
  screenshot or recording for a visible change. Browser mode is never native
  evidence.
- **Risk and rollback** - what breaks if this is wrong, and how to undo it.
- **Issues** - link the owning issue. Use a closing keyword (`Closes #37`) only
  when merging this pull request genuinely finishes that issue, including its
  manual gate. Otherwise write `Refs #37`.

### Draft versus ready for review

- Open as a **draft** while the branch is still moving, while CI has not yet
  passed at the current head, or while a required manual gate is outstanding.
- Mark **ready for review** when the description is complete, CI is green at the
  exact head, and every claim in the description is true at that head.
- A pull request that goes back to being incomplete goes back to draft.

### Generated and maintenance pull requests

A `[bot]` author is exempt from the title check. A human who adopts a bot's
branch owns its title from that point on. Routine maintenance still uses the
template; "Manual Windows validation" is answered with `Not applicable - no
visible behavior change`, never left blank.
