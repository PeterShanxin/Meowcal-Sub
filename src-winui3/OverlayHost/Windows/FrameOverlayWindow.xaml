<Window
    x:Class="OverlayHost.Windows.FrameOverlayWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <!-- Transparent root grid covering entire virtual screen -->
    <Grid x:Name="RootGrid" Background="Transparent">
        <!-- Canvas for overlay content (region borders, etc.) -->
        <Canvas x:Name="OverlayCanvas" />

        <!-- Subtitle panel with acrylic background (positioned below capture region) -->
        <Grid x:Name="SubtitlePanel"
              HorizontalAlignment="Left"
              VerticalAlignment="Top"
              Visibility="Collapsed">
            <Border CornerRadius="8" Padding="16,12">
                <Border.Background>
                    <AcrylicBrush TintColor="Black"
                                  TintOpacity="0.7"
                                  FallbackColor="#B3000000" />
                </Border.Background>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Translated subtitle text -->
                    <TextBlock x:Name="SubtitleText"
                               Grid.Column="0"
                               TextWrapping="Wrap"
                               Foreground="White"
                               FontSize="24"
                               FontFamily="Segoe UI"
                               VerticalAlignment="Center"/>

                    <!-- Settings button -->
                    <Button x:Name="SettingsButton"
                            Grid.Column="1"
                            Content="⚙"
                            FontSize="20"
                            Background="Transparent"
                            BorderThickness="0"
                            Margin="12,0,0,0"
                            VerticalAlignment="Center"
                            Click="SettingsButton_Click"/>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Window>
